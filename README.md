2026

how to run a new build:
- npm install 
- npm run build 

the `/dist` is the build itself, with the HTML/CSS/JS

if you are developer and want to change something, use npm run dev for hot reload. 
