---
interface Props {
  as?: 'h1' | 'h2';
  class?: string;
}

const { as: Tag = 'h2', class: className = '' } = Astro.props;
---

<Tag
  class:list={[
    'font-display font-black text-gray-900 tracking-tight',
    className,
  ]}
>
  <slot />
</Tag>
